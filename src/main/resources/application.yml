server:
 port: 8080

spring:
  h2:
    console:
      enabled: true

  datasource:
    url: jdbc:h2:mem:usuarios
    username: sa
    password:
    driverClassName: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

logging:
 file:
  name: ./logs/evaluacion-java-0.0.1-SNAPSHOT.log